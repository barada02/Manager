agent_environment: Manager
agent_name: hackMe
description: 'Example LangGraph agent. Invoke: curl -X POST <url> -H "Authorization:
  Bearer $DIGITALOCEAN_API_TOKEN" -H "Content-Type: application/json" -d ''{"prompt":
  "hello"}'''
entrypoint_file: main.py
